#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "3-calc.h"

/**
 * op_add - perfoms the addition operation
 * @a: first number.
 * @b: second nunber.
 * Return: result.
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_add - perfoms the substraction operation
 * @a: first number.
 * @b: second nunber.
 * Return: result.
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_add - perfoms the multiplication operation
 * @a: first number.
 * @b: second nunber.
 * Return: result.
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_add - perfoms the divition operation
 * @a: first number.
 * @b: second nunber.
 * Return: result.
 */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_add - perfoms the modulation operation
 * @a: first number.
 * @b: second nunber.
 * Return: result.
 */
int op_mod(int a, int b)
{
	return (a % b);
}
